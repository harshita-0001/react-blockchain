const Swap=({title,content})=>{
   // title and content get dyanamically
   return(
      <div>
        <p style={{margin:"0px",fontSize:"12px"}}>{title}</p>
        <h6>{content}</h6>
      </div>
   )
}

export default Swap;